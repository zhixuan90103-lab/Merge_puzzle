# 原型变更汇总

| 元数据 | 内容 |
|--------|------|
| 文档状态 | **与代码同步** |
| 版本标签 | **原型 v0.6** |
| 规则真源 | [GAME_RULES.md](./GAME_RULES.md) |
| 架构 | [ARCHITECTURE_GAME.md](./ARCHITECTURE_GAME.md) |

---

## v0.6 一览（当前）

| # | 主题 | 说明 | 代码 |
|---|------|------|------|
| 1 | **主线定义** | 合成 → 推杂色 → 单色满盘 64 → 下一关 | 规则/文档 |
| 2 | **自由合后形** | 2V 任意 w×h；优先 A∪B 实心摆放 | `shapes` `merge` `dropResolve` |
| 3 | **色 + 流放** | 同色才合；盘上无该色则不出该色 | `types` `spawn` `progress` |
| 4 | **关卡色种表** | 1–3 关 2 色；4+ 逐步到 5 | `progress` `deal` `game` |
| 5 | **推** | 仅 **&lt; 2V**；同体积不可推 | `merge` `dropResolve` |
| 6 | **出块** | 配对优先；防秒死；有空尽量出 | `spawn` `deadlock.hasSustainablePlay` |
| 7 | **裁切保色** | 半出盘 value 变、color 不变 | `board` `timeline` `view` |
| 8 | 拖合一体 | G 落点 / F 意图 / T* 预览 | `dropResolve` `view` |

---

## 关键玩家规则（v0.6）

1. **合**：同色同数 + 朝向相容 + 重叠≥1；外形跟摆放走。  
2. **推**：合后 2V 推更小块；杂色靠推离盘。  
3. **流放**：盘上没了某色 → 不再刷它；剩一色冲 64。  
4. **过关**：64 满屏 → 清盘下一关。  
5. **失败**：不能搬且不能合。

---

## 历史摘要

| 版本 | 要点 |
|------|------|
| 0.3 | 连锁推、连续动画、结算裁切 |
| 0.4 | 拖合一体、重叠才合、意图生长 |
| 0.5 | 色种、满屏循环、出块带色 |
| **0.6** | 主线收敛；自由合后形；流放出块；关卡表；同体积不互推 |

---

## 刻意未做

- 消行 / 限时 / 分数破纪录  
- 主动丢弃手势  
- Android  
- 完整 haptics 事件表  
